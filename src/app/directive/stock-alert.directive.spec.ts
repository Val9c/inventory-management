import { StockAlertDirective } from './stock-alert.directive';

describe('StockAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new StockAlertDirective();
    expect(directive).toBeTruthy();
  });
});
